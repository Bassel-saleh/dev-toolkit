## Skip CI (Github Actions)

```bash
git commit -m 'Hello World [skip ci]'
```

#### To skip a Continuous Integration (CI) pipeline run, include a specific keyword like [skip ci] or [ci skip] in your commit message.

- **`-m`** : adds a comment message.
